---
description: Switch the active Firebase project
---

This workflow switches the currently active Firebase project using the repository's custom script.

1. Switch project
   Replace `[projectId]` with your desired project ID.
   // turbo
   pnpm run switch-project [projectId]
